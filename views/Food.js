import React from 'react';
import {StyleSheet, Text, View } from 'react-native';

const Food = ({
    params,
}) => (
    <View>
        <Text>Food</Text>
    </View>
);

export default Food;
const styles = StyleSheet.create({
    
});